# .env.template
# 示例环境变量模板（不要在版本控制中放置真实密钥）。
# 说明：项目使用 pydantic-settings，Settings.model_config.env_nested_delimiter="__"

# --------------------------
# Agent 配置（可为不同 agent 指定类型和其专属 LLM 配置）
# 顶层键为 AGENTS，后面接 agent key（例如 executor、orchestrator）
# 每个 agent 包含：name、agent_type、llm（支持多个命名 LLM 配置）
# --------------------------
# 为 executor 定义基本信息
AGENTS__executor__name=executor
AGENTS__executor__agent_type=executor
# 为该 agent 指定默认的 LLM 配置
AGENTS__executor__llm__default__provider=openai
AGENTS__executor__llm__default__api_key=REPLACE_WITH_EXECUTOR_API_KEY
AGENTS__executor__llm__default__base_url=https://open.bigmodel.cn/api/coding/paas/v4
AGENTS__executor__llm__default__model=GLM-4.6
AGENTS__executor__llm__default__max_tokens=8192
AGENTS__executor__llm__default__timeout=60

# 可以为同一个 agent 指定多个 LLM 配置，agent 会根据工作流的阶段选择特定的 LLM 配置
# AGENTS__executor__llm__thinking__provider=anthropic
# AGENTS__executor__llm__thinking__api_key=REPLACE_WITH_ANTHROPIC_API_KEY
# AGENTS__executor__llm__thinking__base_url=https://api.anthropic.com
# AGENTS__executor__llm__thinking__model=claude-3-5-sonnet-20241022
# AGENTS__executor__llm__thinking__max_tokens=4096
# AGENTS__executor__llm__thinking__timeout=30

# 为 orchestrator 定义基本信息
# AGENTS__orchestrator__name=orchestrator
# AGENTS__orchestrator__agent_type=orchestrator
# # 为该 agent 指定默认的 LLM 配置
# AGENTS__orchestrator__llm__default__provider=anthropic
# AGENTS__orchestrator__llm__default__api_key=REPLACE_WITH_ORCHESTRATOR_API_KEY
# AGENTS__orchestrator__llm__default__base_url=https://api.anthropic.com
# AGENTS__orchestrator__llm__default__model=claude-3-5-sonnet-20241022
# AGENTS__orchestrator__llm__default__max_tokens=8192
# AGENTS__orchestrator__llm__default__timeout=60

# --------------------------
# Embeddings 配置（嵌入模型配置，用于向量嵌入任务）
# 顶层键为 EMBEDDINGS，后面接嵌入模型名称
# --------------------------
# 默认嵌入模型配置
EMBEDDINGS__default__provider=openai
EMBEDDINGS__default__api_key=REPLACE_WITH_OPENAI_API_KEY
EMBEDDINGS__default__base_url=https://api.openai.com/v1
EMBEDDINGS__default__model=text-embedding-3-small
EMBEDDINGS__default__max_tokens=8192
EMBEDDINGS__default__timeout=30

# 其他嵌入模型示例
# EMBEDDINGS__fast__provider=zhipu
# EMBEDDINGS__fast__api_key=REPLACE_WITH_ZHIPU_API_KEY
# EMBEDDINGS__fast__base_url=https://open.bigmodel.cn/api/paas/v4/embeddings
# EMBEDDINGS__fast__model=embedding-2
# EMBEDDINGS__fast__max_tokens=8192
# EMBEDDINGS__fast__timeout=15

# --------------------------
# 全局应用配置
# --------------------------
# 日志级别（DEBUG/INFO/WARNING/ERROR/CRITICAL）
LOG_LEVEL=INFO
# 日志文件路径
LOG_FILE=logs/app.log
# 任务失败重试次数
RETRY_ATTEMPTS=3

# --------------------------
# LLM 配置示例（支持的提供商）
# --------------------------
# OpenAI
# provider=openai
# base_url=https://api.openai.com/v1
# api_key=sk-*
# model=gpt-4/gpt-3.5-turbo/gpt-4-turbo

# Anthropic Claude
# provider=anthropic
# base_url=https://api.anthropic.com
# api_key=sk-ant-*
# model=claude-3-5-sonnet-20241022/claude-3-haiku-20240307

# Zhipu AI (智谱清言)
# provider=zhipu
# base_url=https://open.bigmodel.cn/api/paas/v4
# api_key=*
# model=GLM-4.6/GLM-4-Flash/GLM-3-Turbo

# OpenAI Compatible (包括本地部署)
# provider=openai
# base_url=http://localhost:8000/v1
# api_key=any-key-or-empty
# model=qwen-72b-chat/llama-2-70b-chat

# --------------------------
# 配置说明
# --------------------------
# 1. LLM 配置结构：
#    - provider: LLM 提供商 (openai, anthropic, zhipu, 等)
#    - api_key: API 密钥
#    - base_url: API 基础 URL
#    - model: 模型名称
#    - max_tokens: 最大令牌数 (1-128000)
#    - timeout: 请求超时时间 (1-600秒)
#    - extra_body: 额外的请求体参数 (JSON格式，可选)

# 2. 嵌套分隔符：
#    - 使用 "__" 作为嵌套分隔符
#    - 例如：AGENTS__executor__llm__default__provider 表示 agents.executor.llm.default.provider

# 3. 获取配置：
#    - Agent 配置：get_settings().get_agent_config("executor")
#    - LLM 配置：agent_config.get_llm_config("default")
#    - 嵌入配置：settings.embeddings.get("default", LLMConfig())

# 4. 安全建议：
#    - 请勿将真实密钥提交到仓库
#    - 生产环境请通过 Secret 管理（K8s Secret / Vault / CI secrets）
#    - 使用不同的 API Key 隔离不同 Agent 的权限

# 这是模板文件，复制并重命名为 .env 或在部署平台上注入对应的环境变量
