# .env.template
# 示例环境变量模板（不要在版本控制中放置真实密钥）。
# 说明：项目使用 pydantic-settings，Settings.model_config.env_nested_delimiter="__"

# --------------------------
# Agent 配置示例（可为不同 agent 指定类型和其专属 LLM 配置）
# 顶层键为 AGENTS，后面接 agent key（例如 executor_agent、planner_agent）
# 示例：为名为 "executor_agent" 的 agent 指定配置并为其定义 llm 映射
# --------------------------
# 为 executor_agent 定义基本信息
AGENTS__executor_agent__name=demo
AGENTS__executor_agent__agent_type=EXECUTOR
# 为该 agent 指定默认的 LLM（使用 agent 下的 llm 映射）
AGENTS__executor_agent__llm__default__provider=openai
AGENTS__executor_agent__llm__default__model=GLM-4.6
AGENTS__executor_agent__llm__default__base_url=https://open.bigmodel.cn/api/coding/paas/v4
AGENTS__executor_agent__llm__default__api_key=REPLACE_WITH_EXECUTOR_API_KEY

# 为 planner_agent 定义基本信息
# AGENTS__planner_agent__name=planner_agent
# AGENTS__planner_agent__agent_type=PLANNER
# # 为该 agent 指定默认的 LLM（使用 agent 下的 llm 映射）
# AGENTS__planner_agent__llm__default__provider=anthropic
# AGENTS__planner_agent__llm__default__model=claude-v1
# AGENTS__planner_agent__llm__default__base_url=https://open.bigmodel.cn/api/coding/paas/v4
# AGENTS__planner_agent__llm__default__api_key=REPLACE_WITH_PLANNER_API_KEY

# --------------------------
# 其它全局配置示例
# --------------------------
# 日志级别（DEBUG/INFO/WARNING/ERROR/CRITICAL）
LOG_LEVEL=INFO
# 日志文件路径
LOG_FILE=logs/app.log
# 任务失败重试次数（create_*_scheduler 中的 max_error_retry）
RETRY_ATTEMPTS=3

# --------------------------
# 说明与建议
# --------------------------
# - 请勿将真实密钥提交到仓库；在本地开发可使用 .env（被 .gitignore 忽略）
# - 生产环境请通过 Secret 管理（K8s Secret / Vault / CI secrets）注入环境变量
# - 当同时存在 JSON 注入与逐项注入时，逐项注入通常会覆盖文件中或 JSON 的设置
# - 在代码中通过 get_settings().get_llm_config("executor") 等方法获取命名配置

# 这是模板文件，复制并重命名为 .env 或在部署平台上注入对应的环境变量
